package utils;

/**
 * Enumeration of different log levels. Used to determine which messages get logged
 * @author Erkka Nurmi
 *
 */
public enum LogLevel {
	TRACE,
	DEBUG,
	ERROR,
	INFO,
	WARNING,
	CRITICAL
}
